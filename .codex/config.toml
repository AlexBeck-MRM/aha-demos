# Repository-specific Codex behavior is intentionally defined in AGENTS.md.
# Keep this file valid but minimal unless a concrete Codex config override is needed.

approval_policy = "on-request"

sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = true
