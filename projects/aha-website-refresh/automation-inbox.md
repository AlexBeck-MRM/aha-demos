# Automation Inbox

## Job
- Track owner decisions and blocked follow-ups from maintenance passes.
- Keep items compact.
- Remove items only after the decision is logged elsewhere.

## Open Items
- `2026-06-17`: Run any Figma-enabled header sync for `Colour Scheme` and `Illustration Style` outside this no-Figma automation.
- `2026-06-17`: Regenerate proof icons through raster image generation before any production icon redraw.
- `2026-06-19`: Decide whether `output/playwright/criticalmass/` should move to `reference/evidence/` or whether `/Users/alexanderbeck/Projects-code/cm-internal-design-tokens/docs/aha-design-system/site/aha-token-system-guide/design.md` should stop treating scratch captures as durable evidence.

## Closed Items
- `2026-06-17`: `npm run clean` policy resolved. Keep `output/` disposable. Copy generated assets that should remain findable to `design/generated/` before clean commands.
- `2026-06-25`: Repo-housekeeping route and disposable-root policy resolved for this cleanup pass. `.gitignore`, `npm run clean`, and the verifier now agree on `.artifacts/`, `.playwright-cli/`, `.playwright-mcp/`, `.tmp/`, `output/`, `tmp/`, and `.aha-server.pid`.
