# Slide Archive

Historical slide-system versions are kept here so the active `slides/` folder stays focused on the current v1.6 work.

- `v1.1/` spec snapshot
- `v1.3/` deck and spec
- `v1.4/` deck and spec
- `v1.5/` deck, tokens, and spec
